name: proj_curso
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0+4
  path_provider: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  
 

flutter:
  uses-material-design: true
  assets:
  - assets/books/itacoisa.png
  - assets/books/domcasmurro.png
